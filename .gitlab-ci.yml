---
image: node

stages:
  - release

publish:
  stage: release
  before_script:
    - npm install -g semantic-release@v17 @semantic-release/gitlab
    - npm install
  script:
    - npm run build:prod
    - semantic-release
  only:
    - master
    - beta
  tags:
    - deploy
